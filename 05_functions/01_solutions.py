# square of a number
# def is used for function definition in Python
# () => this is parenthesis or placeholder rather than telling it as a arguements are stored we can say it is placeholder
# return => this is used to return a value from a function
def square(number):
    return number ** 2

result = square(4)
print(result)